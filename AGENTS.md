# 開発方針および実装履歴 (Antigravity用設定)

## 基本方針
- **絶対厳守**: ユーザー（カオルさま）の指示を正確に遂行すること。
- **勝手な変更の禁止**: 「良かれと思って」の独自判断によるアレンジや追加実装、レイアウト変更を行わないこと。
- **事前の説明**: 修正や実装を行う際は、どのような変更を行うのかをカオルさまに対して十分に説明し、合意を得ながら進めること。

## 直近の実装・修正内容まとめ
1. **検索機能・UIの修正**
   - 検索結果の件数などを表示するヘッダーパネルのUIを調整（stickyによる追従とドロップシャドウ等の視認性改善）。
   - データセット切り替え時や、検索バーのクリア（Xボタン）押下時に、検索クエリ（`searchQuery`, `searchInput`）が確実にリセットされるよう挙動を修正。

2. **フォント切り替え機能の実装**
   - ヘッダー右上にフォント選択用のプルダウンを追加。
   - 「GOTHIC（ゴシック）」「MARU（丸ゴシック）」「MEIRYO（メイリオ）」「MONO（等幅）」の4種から選択でき、アプリ全体のフォント（`data-font`）が動的に切り替わるよう実装。

3. **数字のレイアウト崩れ防止（tabular-nums）**
   - プログラム用等幅フォントの強制適用を解除したことで「0」が「8」に見える問題を解決しつつ、全体のCSS（`body`）に `font-variant-numeric: tabular-nums;` を設定。これにより、どのフォントを選んでも数字の幅が固定され、株価などの桁数がずれるレイアウト崩れを防止。

4. **ヘッダーボタンのレイアウト調整**
   - 「CANVAS」や「THEME」を切り替えるボタンにおいて、選択肢の文字数の違いによって生じていたボタン幅のバラつきを修正。すべて一定の幅（`w-12`）に固定することで、UIのズレ（気持ち悪さ）を解消。

5. **モーダルダイアログの z-index 修正**
   - フルスクリーン表示（`z-[100]`）時にファイル名の編集（鉛筆マーク）を押すと、ダイアログが裏側に隠れてしまう不具合を修正。
   - すべてのモーダルダイアログの `z-index` を `z-[110]` に引き上げ、常に最前面に表示されるように改善。

6. **データセット（フォルダー）削除機能の修正**
   - iFrame 環境下で `window.confirm` がブロックされてしまい、削除ボタンが反応しない問題を修正。
   - `window.confirm` の代わりに、アプリ専用の削除確認モーダルダイアログ（DELETE DATASET）を実装し、正常に消去できるように改善しました。

7. **画像ホバー時のレイアウト変更**
   - 画像をホバー（選択）した際、従来は枠（フレーム）全体が少しズームアウト（拡大）していましたが、枠の大きさは固定したまま中の画像だけがズームするように修正しました。これにより、ホバー時の違和感をなくしました。

8. **フォルダードロップによるデータセット自動作成・登録機能の実装**
   - ファイルの入ったフォルダーを画面にドラッグ＆ドロップした際、自動的にフォルダー内のファイルを抽出し、フォルダー名を元に新しいデータセット（リスト）を作成して登録するように修正。
   - 既存のリスト（空のリストなど）を選択している状態でフォルダーをドロップした場合は、そのリストへファイルを直接追加できるように改善。

9. **ドロップ時の新規作成・追加の選択機能**
   - ファイルの入ったフォルダーをドラッグ＆ドロップした際、常に既存のリスト（アクティブなリスト）に追加されてしまう仕様を改善。
   - データセットリストの下部に専用のドロップエリアを配置し、「現在のリストに追加（ADD TO ACTIVE）」と「新しいリストとして作成（CREATE NEW）」を選択できるようにUIを変更。また、大きな画面でのドラッグ＆ドロップ時は既存のリストへの追加（ADD TO ACTIVE）をデフォルトの動作とし、フォルダー名で新規作成する場合はサイドバーのドロップエリアを利用するように改善。

10. **データセット新規作成時のUI更新修正**
    - サイドバーの「フォルダー名でリストを作成（CREATE BY FOLDER）」や画面全体のドロップオーバーレイから新規データセットを作成した直後に、新しいデータセットが選択状態になるものの、中の画像が即座に表示されない（別のリストを再選択する必要がある）不具合を修正。
    - データセットのID切り替え（非同期）を待たずに、処理完了時に強制的に画像をロードするように挙動を改善。

11. **ドロップエリアの枠線の視認性改善**
    - ドロップエリアの点線枠が薄くて見えにくかったため、ボーダーの色を `border-text-muted/50` から `border-text-muted` へ変更し、少しだけ濃く（見やすく）なるように調整。

12. **ヘッダーレイアウトの最適化**
    - サイドバーを閉じた状態でも画像の表示サイズと隙間を調整できるよう、それらのスライダーをサイドバー（FORMATION ENGINE）からヘッダー上部に移動。
    - ヘッダーのスペースを確保するため、THEME（テーマ）の切り替えを5つのボタンから、クリックするごとに切り替わる1つのトグルボタンに変更。

13. **ヘッダーUI・操作感のさらなるブラッシュアップ**
    - テーマ切り替えボタンの幅を固定（固定幅＋左寄せ）し、文字数（テーマ名）が変わっても全体のレイアウトがズレないように修正。
    - ボタン全般（SolidButton）のクリック時のアニメーションを調整。フワッとする（transition-colors や y: 1）要素を排除し、スケールダウンのみを高速化（duration 0.05）することで、「スパスパッ」と反応するシャープな押し心地に改善。
    - 画像サイズ（SIZE）および隙間（GAP）スライダーの幅を長く（w-32）広げ、調整時の「遊び」と操作性を向上。
    - スライダー操作時に画像サイズが変わる際、Framer Motion の `layout` アニメーションが全画像に対して都度計算されていたため、反応が遅延していた問題を解消。`layout` プロパティを除外することで CSS による即時リサイズとし、ツマミの動きに遅延なく追従するようにパフォーマンスを劇的に改善。

14. **アニメーションとボタンの調整**
    - 画像サイズおよび隙間の変更アニメーションについて、PWA版と同等の滑らかな動き（`layout`アニメーション有効状態）が最適であると再確認したため、元の仕様へ差し戻し。
    - 画像サイズ（SIZE）および隙間（GAP）スライダーの幅をさらに長く（w-40）広げ、調整時の「遊び」と操作性を向上。
    - ボタン全般（SolidButton）のクリック時の `whileTap` によるスケールダウン（グニャッと沈み込む動き）を完全に削除。クリックしてもUIが動かず、シャープな押し心地になるように改善。

15. **スライダー数値の単位追加**
    - 画像サイズおよび隙間スライダーの数値表示部分に「px」の単位を追加し、分かりやすさを向上。同時に3桁表示になってもレイアウトが崩れないよう幅を微調整。
16. **検索結果のレイアウト修正 (SQUAREおよびSCATTERモード)**
    - SQUARE（正方形グリッド）モードで検索した際、検索結果の画像が縦1列に並んでしまうレイアウト崩れを修正し、通常のグリッドとして綺麗に並ぶように改善。
    - SCATTER（散布）モードで検索した際、検索結果のフォルダー名などのグループ枠が背景に表示されたままになってしまう仕様を見直し、SCATTERモード中は純粋に検索結果の画像のみが散らばる（グループ枠は出さない）ように挙動を最適化。
17. **ドラッグ＆ドロップ時の読み込み上限の修正**
    - フォルダをドラッグ＆ドロップした際、ブラウザの `readEntries` の仕様により1回につき100件までしか読み込まれない問題があったため、再帰的に呼び出してフォルダ内の全ファイルを最後まで取得するように修正。これにより、100件以上の画像を含むフォルダでも一度に登録可能になりました。
18. **サイドバーの横幅リサイズ機能の実装**
    - サイドバーとメインコンテンツの境界線をドラッグすることで、サイドバーの横幅を調整（拡張）できるように機能を実装。
    - 最低幅を現在の300pxとし、それ以上に広げることが可能。ドラッグした幅のデータはローカルストレージに保存され、次回起動時も保持されるように改善。
    - 境界線（リサイズハンドル）にマウスを合わせた際のホバーエフェクトは、あまり目立ちすぎないように控えめな色（hover:bg-white/5）に設定。左右どちらに配置しても正常にリサイズできるように対応。
18. **サイドバーの横幅リサイズ機能の改善**
    - リサイズ時の上限を650pxに制限し、広げすぎを防ぐよう修正。
    - リサイズ中にデータセットリストの文字列が拡縮して歪む（ウニャウニャする）現象について、Framer Motion の Reorder.Item に対して `layout="position"` を指定し、横幅のレイアウトアニメーションを無効化することで解消。
19. **画像一覧のスクロールボタンのアニメーション修正**
    - 画面右下の方向キー（上下ボタン）によるスクロール操作について、最初の一回の移動の後にインターバル（一旦止まるような動き）が発生していた仕様を見直し。
    - 押し続けている間は滑らかに一定速度でスクロールし続けるように挙動を改善。
20. **フルスクリーン縦画面（ポートレート）モードの追加**
    - モニターを90度回転させた際に利用するための「PORTRAIT MODE」を実装。
    - フルスクリーン時の右上に切り替えボタン（スマートフォン/モニターアイコン）を追加し、ボタン一つでUI全体と画像表示が-90度回転するように設定。
    - UIが回転した状態でも、キーボード操作（上下左右の矢印やテンキー）と画像移動方向（パン操作）がユーザーの見た目通りに動くように、内部のキーマッピングを補正。
21. **ポートレートモードと操作の改善**
    - ポートレートモード（縦画面）の回転を2段階（-90度と90度）に切り替えられるようにし、左右どちらにモニターを回転させても対応できるように改善。
    - 画像ズーム中のキーボード操作について、「左右の矢印キー」は画像の移動（パン）ではなく、常に「次/前の画像」への切り替えとして機能するように仕様を修正。パン操作は上下矢印やテンキーで行い、回転方向に応じて直感的に動くように補正。
    - フルスクリーン時の右上ボタン（閉じる、フルスクリーン化など）について、画像が黒や白の場合に見えなくなるのを防ぐため、円形の半透明背景（backdrop-blur）を追加して視認性を向上。
22. **キーボードナビゲーションの強化（マウス代替操作）**
    - ポートレート（縦画面）モード時など、マウス操作が困難な状況でもキーボードだけで快適に操作できるよう、以下のショートカットを追加。
      - 一覧画面：左右矢印キー（`←` `→`）で画像のフォーカス移動、`Enter`でフルスクリーン表示。
      - スクロール追従：キーボードで画像を選択した際、その画像が常に画面内に見えるように自動スクロール。連続移動時に突っかかり（カクつき）が発生しないよう、スムーズスクロール（behavior: 'smooth'）ではなく即時スクロール（behavior: 'auto'）に変更。
      - モード切替等：`P`キーでポートレートモードの回転切替、`F`キーでボーダレスフルスクリーンの切替、`R`キーで画像の90度回転、`H`キーで画像の左右反転。各ボタンのツールチップにもショートカットキーと日本語の説明を表示。
      - UI表示切替：フルスクリーン時に`U`キーでボタンやファイル名などのUI表示/非表示を切り替え。
      - 閉じる：フルスクリーン時に`Esc`または`Backspace`キーで一覧画面に戻る。

23. **同じ名前の画像がドロップされた時の更新（上書き）機能**
    - 同じ名前の画像（すでにリストに登録されている画像）をドラッグ＆ドロップした際、これまでは自動的にスキップ（弾かれる）仕様でしたが、専用の確認モーダルダイアログを表示して、ユーザーが「更新する（UPDATE）」か「キャンセル（CANCEL）」を選択できるように改善。
    - 「更新する」を選択した場合は、以前の画像の並び順（orderIndex）を保持したまま、新しい画像データで上書き（再登録）されるように最適化。