# 開発方針および基本ルール (Antigravity用設定)

## 基本方針
- **絶対厳守**: ユーザー（カオルさま）の指示を正確に遂行すること。
- **勝手な変更の禁止**: 「良かれと思って」の独自判断によるアレンジや追加実装、レイアウト変更を行わないこと。
- **事前の説明**: 修正や実装を行う際は、どのような変更を行うのかをカオルさまに対して十分に説明し、合意を得ながら進めること。

## 呼称・対話スタイル
- ユーザーを呼ぶ際は「カオルさま」とし、敬意を払いながら距離感を保つこと。
- 一人称や会話の雰囲気を尊重し、過度な開発者専門用語やマーケティング的表現を避けること。

## アプリケーション基本仕様（現在の実装状況）
- **UI・レイアウト**:
  - ヘッダー：検索バー、画像サイズ(SIZE)・隙間(GAP)スライダー、フォント切替(GOTHIC/MARU/MEIRYO/MONO)、テーマ切替(固定幅トグル)、キャンバス切替。
  - 数字表示には `font-variant-numeric: tabular-nums;` を適用し桁ズレを防止。
  - ボタン（SolidButton）は余計な沈み込みアニメーションを排除し、シャープな押し心地を維持。
- **サイドバー**:
  - 横幅リサイズ対応（300px〜650px、ドラッグで調整可能・LocalStorage保存）。
  - 各データセットにお気に入り設定（☆）機能。
  - フォルダーのドラッグ＆ドロップで新規作成（CREATE BY FOLDER）または既存リストへの追加。
  - データセット選択式エクスポート（EXPORT DATA）モーダル対応。
  - **COMMAND INFO（コマンド一覧）パネル**: サイドバー最下部にショートカットキー一覧（一覧画面 / 全画面＆スライドショー）を表示。開閉状態・ドラッグ並び替え対応。
- **一覧画面・操作**:
  - レイアウトモード（SQUARE / MASONRY / SCATTER）。
  - 画像の向きフィルター（ORIENT: ALL / PORTRAIT / LANDSCAPE）。縦横比に応じてSIZE/GAPを個別保持。
  - シークレット（Secret / 目のアイコン）機能：一時非表示および復元（REVEAL）対応。
  - 複数選択（Editモード）：移動、コピー、お気に入りコピー、シークレット化、完全削除（確認モーダル付き）。
  - **一覧の自動スクロール（Auto Scroll）機能**:
    - 画面右下のフローティングコントローラー（TOP / UP / AUTO・STOP / DOWN / BOTTOM / 速度 1x〜4x 切り替え）。
    - 動作速度（1x: 100px/s, 2x: 250px/s, 3x: 500px/s, 4x: 1000px/s、Tキーで1x〜4xを順次切り替え）。
    - Spaceキーで自動スクロールの開始・停止トグル（停止前の方向で再開）。
    - キーボード上下矢印キー（↑ / ↓）のワンプッシュでその方向への自動スクロール開始＆動作中の方向切り替え、長押しで一時加速に対応。
    - スクロール動作中に画像や画面をクリックした場合、自動スクロールが1度停止（停止後のクリックで画像選択/拡大展開）。
    - ボタン類のフォーカス枠（黒枠線）の抑制。
- **フルスクリーンモード**:
  - ポートレート（縦画面）モード（Pキー、90度/-90度回転、キーボード操作方向の自動補正）。
  - Zoom to Fill機能（Zキー：画面いっぱいフィット/リセット）。
  - キーボード操作：左右矢印で画像送り、上下矢印/テンキーでパン、Shift+左右矢印でデータセット間移動、Esc/Backspaceで閉じる、UキーでUI表示切替、Rキーで90度回転、Hキーで左右反転。
  - **スライドショー機能**:
    - 画面左下のピル型コントローラー（再生/停止、再生方向切替 FWD » / « REV、インターバル秒数切替 1s/2s/3s/5s/8s/10s）。
    - ショートカットキー対応：Spaceキーで再生/停止、Sキーで方向切り替え、Tキーでインターバル秒数切り替え。
- **ダイアログ・削除**:
  - iFrame対応のため `window.confirm` は使用せず、専用の確認モーダル（削除確認・同名ファイル上書き確認等）を使用。モーダルのz-indexは `z-[110]`。
